【14:30】feat: 添加手动检查更新功能（自定义服务器 + 下载 exe 安装包）
- 新增 `src/updater.py`：UpdateConfig（配置读写）、UpdateChecker（检查更新线程）、UpdateDownloader（下载线程）
- 新增 `src/update_dialog.py`：更新对话框 UI（配置服务器地址、检查更新、显示 changelog、下载进度、启动安装包）
- 修改 `src/constants.py`：新增 UPDATE_CONFIG_FILE 常量
- 修改 `src/playlist_sidebar.py`：设置分组新增"检查更新"导航项 + update 图标
- 修改 `src/playlist_view.py`：接收 UpdateConfig，处理检查更新点击事件
- 修改 `src/main_window.py`：初始化 UpdateConfig 并传递给 PlaylistView

【22:31】refactor: 用 WM_NCHITTEST 原生消息替代 startSystemResize，窗口拖拽调整大小完全由系统处理；标题栏拖动改用 startSystemMove() 替代手动计算
- 修改 `src/main_window.py`：新增 nativeEvent 处理 WM_NCHITTEST，移除旧的 _get_resize_edge / _update_resize_cursor / _to_qt_edges / startSystemResize 代码
- 修改 `src/title_bar.py`：移除手动拖拽逻辑，改用 startSystemMove()
